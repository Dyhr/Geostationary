[package]
name = "network"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = { version = "0.12", features = [] }
bevy_quinnet = { version = "0.6" }
serde = { version = "1.0", features = ["derive"] }
